services:
  - type: web
    name: scriptbot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: TOKEN
        value: YOUR_BOT_TOKEN